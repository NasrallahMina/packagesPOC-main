import React, { useState } from "react";

const Filtration = (props)=>{
    let onInputChnage = (e)=>{
        props.handleChange(e.target.value)
    }
    return(
        <div>
            filtration by title : <input type="text" placeholder="Title" onChange={onInputChnage} />
        </div>
    )
}

export default Filtration;